FROM node:20-slim

WORKDIR /app

# Install dependencies first (optimization for layer caching)
COPY package*.json ./
RUN npm install

# Copy the rest of the application
COPY . .

# Expose the Vite port
EXPOSE 5173

# Start Vite with --host to allow external access
CMD ["npm", "run", "dev", "--", "--host"]
